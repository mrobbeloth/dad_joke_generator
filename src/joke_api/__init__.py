"""Joke_API: AWS Lambda backend for the Dad Joke Generator.

This package contains the stateless Python modules that compose the
Joke_API Lambda. Each module corresponds to a component boundary in
``design.md`` so the pipeline stages remain independently testable.
"""
